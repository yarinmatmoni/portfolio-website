/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	swcMinify: true,
	images: {
		domains: ['yarinmatmoni.github.io/portfolio-website'],
	},
};

module.exports = nextConfig;
